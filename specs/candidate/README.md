# Candidate specifications (non-normative)

Documents in this directory are **candidates only**. They are not part of the normative conformance stack (`QSM-ARCH`, `TSS`, versioned schemas, or public ontology files).

- **Status:** Each file is marked `candidate` until accepted through the standard's governance process.
- **Conformance:** Nothing here is a conformance requirement until promoted to normative spec text.
- **Review gate:** The inputs/encoding/retention sub-model is gated on the internal ontology review scheduled for **2026-10-01** (`inputs-memory-allocation-ontology.md` in the work-system ADR drafts).

Do not cite candidate text as settled standard language. Do not merge candidate prose into versioned normative specs without an explicit release/amendment.
