{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://stewardshipstandard.org/schemas/record/v1.1.json",
  "title": "TSS Record Schema",
  "description": "An append-only auditable entry describing a stewardship event. Defined by TSS v1.1.1 §5.9 and §9. Domain-specific record types (StewardshipRecord, WellnessRecord, TENURE Record) MUST satisfy these rules.",
  "type": "object",
  "required": ["id", "event_type", "timestamp"],
  "properties": {
    "id": { "type": "string" },
    "event_type": { "type": "string" },
    "timestamp": { "type": "string", "format": "date-time" },
    "actor_id": { "type": "string" },
    "context_id": { "type": "string" },
    "summary": { "type": "string" },
    "details": { "type": "object" }
  }
}
